## 📂 Структура Проекта (Anime Top Website)

---

## 📁 Дерево проекта: Расширенная информация

| Тип | Название | Путь | Краткое Содержание / Назначение |
| :--- | :--- | :--- | :--- |
| **Файл** | `index.html` | `/` | **Главная страница** сайта (точка входа). Содержит основную навигацию и общую информацию. |
| **Файл** | `tasks.md` | `/` | Документ с описанием задач или планом работы по проекту. |
| **Директория** | `ClassWork/` | `/` | Папка для учебных файлов и демонстраций, созданных в ходе выполнения классных работ. |
| Файл | `ClassWork*.html` | `ClassWork/Documents/` | HTML-документы для отдельных практических заданий (Блоки 3, 4, 5, 6, 7, 11). |
| **Поддиректория** | `Scripts/` | `ClassWork/Scripts/` | JavaScript-файлы, разработанные для выполнения заданий классных работ. |
| Файл | `script*.js` | `ClassWork/Scripts/` | JavaScript для задания Блоков7-11. |
| Файл | `style*.css` | `ClassWork/Styles/` | CSS-стили, специфичные для заданий Блоков 4, 5, 6, 7. |
| **Директория** | `media/` | `/` | Общая папка для медиа-контента, используемого на главной странице. |
| Файл | `AnimeeInst.jpg`, `Dushnila.png`, `Re*` | `media/` | Общие изображения (логотипы, баннеры, трейлеры). |
| **Директория** | `pages/` | `/` | Основные страницы сайта, организованные по категориям (жанрам аниме). |
| Файл | `action.html`, `comedy.html`, `fantasy.html`, и т.д. | `pages/` | Страницы-категории по жанрам (Экшн, Комедия, Фэнтези и т.д.) с соответствующими подборками. |
| **Поддиректория** | `medias/` | `pages/medias/` | Внутренние медиа-ресурсы для страниц-категорий. |
| **Поддиректория** | `icons/` | `pages/medias/icons/` | Иконки или мелкие графические элементы для дизайна страниц-категорий. |
| Файл | `Dushnila.png` | `pages/medias/icons/` | Файл иконки (дубликат или версия для страниц). |
| **Поддиректория** | `illustration/` | `pages/medias/illustration/` | **Иллюстрации** и постеры аниме для оформления страниц-категорий. |
| Файл | `86.png`, `Alhimist.png`, `Атака титанов.png`, и т.д. | `pages/medias/illustration/` | Множество иллюстраций аниме (постеры, кадры, обложки). |
| **Директория** | `scr/` | `/` | **Основные ресурсы** для всего проекта (главной страницы и общих стилей/скриптов). |
| Файл | `mediaZ.css` | `scr/` | CSS-файл с **медиазапросами** для адаптивного дизайна. |
| Файл | `script.js` | `scr/` | Основной **JavaScript-файл**, содержащий общую логику и интерактивность (включая переключатель тем и логику из предыдущих ДЗ). |
| Файл | `style.css` | `scr/` | Основной **CSS-файл** для общего оформления, неадаптивных стилей. |
| **Поддиректория** | `feedback/` | `scr/feedback/` | Раздел для форм обратной связи или контактов (вложен в `scr/`). |
| Файл | `feedback.html` | `scr/feedback/` | Страница **"Обратная связь"**. |
| **Поддиректория** | `tests/` | `scr/tests/` | Файлы, используемые для тестирования в браузере. |
| Файл | `test.html` | `scr/tests/` | HTML-файл для запуска **Mocha/Chai тестов** в браузере. |
---

## 📝 Задания по Интернет-технологиям

### 🧱 Блок 2: Базовый HTML-синтаксис

---

#### 💻 Классная работа (Практическое задание: Страница о себе)
* Создать файл **index.html**.
* Указать **doctype** и **язык** документа.
* Добавить в `<head>`: **заголовок вкладки**, **описание страницы**, **иконку** вкладки, **кодировку** (`<meta charset="UTF-8">`).
* Добавить главный заголовок (`<h1>`) с именем/ником.
* Создать раздел «О себе» с `<h2>` и 1–2 параграфами.
* Добавить разделы «Хобби» и «Учёба/Работа» с `<h2>` и описанием.
* Добавить списки: **упорядоченный** (`<ol>`), **неупорядоченный** (`<ul>`), **терминологический** (`<dl>`).
* Вставить изображение (`<img>`) с атрибутами `src` и `alt`.
* Использовать разделение блоков.

#### 🏠 Домашнее задание
* Создать **первую страницу** `index.html`.
* Обязательные элементы: `<!DOCTYPE html>`, `<html lang="ru">`, `<head>`, `<body>`.
* В `<head>`: `charset`, `<title>`, `<meta name="description">`.
* В `<body>`: **один** `<h1>`, **два** абзаца `<p>`.
* Один **неупорядоченный** список (`<ul>`).
* Один **упорядоченный** список (`<ol>`).
* Один пустотный элемент `<hr>` между списками.
* Изображение (`<img>`) с атрибутом `alt`.

---

### 🧱 Блок 3: Семантика и Ссылки

---

#### 💻 Классная работа (Практическое задание: Макет страницы новостей)
* Создать структуру: `<header>`, `<main>`, `<footer>`.
* Внутри `<main>` разместить одну «новость» внутри `<article>`.
* Внутри новости: заголовок `<h2>`, `<abbr>`, параграф с `<time>`, `<blockquote>`.
* Использовать **надстрочный** (`<sup>`) и **подстрочный** (`<sub>`) индекс.
* Добавить **абсолютную** и **относительную** ссылки.

#### 🏠 Домашнее задание (Цель: Семантическая структура и ссылки)
* Главную страницу (`index.html`) обернуть в семантические блоки: **`<header>`**, **`<main>`**, **`<footer>`**.
* Сделать **минимум 2 страницы** и связать их **ссылками** (прямая и обратная).
* Добавить **абсолютную** ссылку.
* Добавить минимум 2 расширенных элемента текста: `<abbr>`, `<time>`, `<sup>`/`<sub>`, `<blockquote>` или `<address>`.

---

### 🧱 Блок 4: Медиа, Таблицы и Формы

---

#### 💻 Классная работа (Практическое задание: Анкета ученика)
* Добавить **видео** или **аудио** с `controls`, альтернативным форматом и подписью.
* Создать **таблицу** «Расписание уроков»: мин. 3x3, `<caption>`, `<thead>`, `<tbody>`.
* Добавить **форму**: поля «Имя», «Почта», **выпадающий список** («Любимый предмет»), кнопка «Отправить».

#### 🏠 Домашняя работа (Цель: Расширить проект, добавив медиа, таблицу и форму)
* На главной странице разместить **видео** и **аудио**.
* На отдельной странице создать **таблицу**: мин. 3x3, заголовки (`<th>`), `<caption>` и `<tfoot>`.
* Сделать страницу «**Обратная связь**» (`contacts.html`).
* **Форма** с мин. 3 полями (имя, email, сообщение). Поля должны быть **обязательными** (`required`).

---

### 🧱 Блок 5: Введение в CSS и Блочная модель (Часть 1)

---

#### 💻 Классная работа (Практическое задание: Оформление списка новостей)
* В HTML создать **ненумерованный список** из 5 новостей.
* **Стилизовать** с помощью CSS (подключить `style.css`):
    * При наведении: строка становится синей, текст белым (`:hover`).
    * Перед каждой строкой списка добавить «+» (`::before`).
    * Первую строку выделить жирным и увеличить шрифт (`:first-child`).
* Применить **блочную модель** к элементам списка (`padding`, `border`, `margin`, `box-sizing`).
* Для всего списка задать рамку и внешний отступ.

#### 🏠 Домашняя работа (Цель: Начать оформление проекта)
* Подключить **внешний CSS-файл** (`style.css`).
* Стилизовать: **заголовок** (цвет/размер), **основной текст** (шрифт), **фон страницы**.
* Применить **псевдоклассы**, **отступы** и **границы**.
* На выбор: **прозрачность** или **тени**.
* Добавить хотя бы **два класса** и **один id** и применить к ним стили.

---

### 🧱 Блок 6: Селекторы и Блочная модель (Часть 2)

---

#### 💻 Классная работа (Задача: Создать «карточку профиля»)
* Сделать блок `<div>` с классом `.profile-card`.
* Внутри разместить: **Заголовок**, **Абзац**, **Картинку** (аватар).
* **Стилизовать**:
    * Размер карточки: ширина 300px, мин. высота 200px.
    * Фон: **градиент** (от синего к фиолетовому).
    * **Скруглённые углы** (`border-radius: 15px`).
    * Текст: белый, центрированный.

#### 🏠 Домашняя работа (Цель: Применить селекторы и блочную модель)
* Добавить в проект:
    * Минимум 2 **селектора атрибутов**.
    * Минимум 3 **псевдокласса** (`:hover`, `:first-child`, `:nth-child(odd)` и т.д.).
    * Минимум 1 **псевдоэлемент** (`::before` или `::after`).
* Сделать минимум один блок с оформленной **блочной моделью** (`padding`, `margin`, `border`, `box-sizing`).
* Поменять **`display`** хотя бы у одного элемента (`block`, `inline-block`, `none`).

---

### 🧱 Блок 7: Компоновка (Flexbox, Grid) и Адаптивность

---

#### 💻 Классная работа (Задача: Макет сайта «Визитка дизайнера»)
* Подобрать **два разных шрифта**.
* Использовать **Flexbox** и **Grid** для расположения элементов.
* Сделать **фиксированную** шапку.
* Использовать **“липкое”** позиционирование (`position: sticky`).
* Использовать **медиазапросы**:
    * Для смартфонов (до 480px): элементы в **одну колонку**.
    * Для ноутбуков (от 769px): **двухколоночный** макет.

#### 🏠 Домашнее задание (Цель: Применить компоновку и адаптивность)
* Использовать **минимум два семейства шрифтов**.
* Оформить макет:
    * `<header>` – меню навигации (**Flexbox**).
    * `<main>` – 2-3 секции с **CSS Grid** (галерея, отзывы).
* Настроить **адаптивность** с помощью **медиазапросов**:
    * 3 точки разрыва (мобильный, планшет, десктоп).
    * Контент и сетка перестраиваются.
* *Задание по желанию*: Добавить **анимацию** появления или плавного изменения цвета.

---

### 🧱 Блок 8: Введение в JavaScript (Переменные, Условия, Циклы, Массивы, Функции)

---

#### 💻 Классная работа (Задача: Проверка заказов кафе)
* Создать переменные (`name`, `averageBill`, `hasDelivery`).
* Вывести информацию о кафе в **консоль** одной строкой.
* Работа с **условиями** (`if...else`) на основе `averageBill`.
* Создать массив `orders` и с помощью **цикла `for`** вычислить сумму заказов > 300.
* Работа с **методами массивов** (`push`, удаление первого элемента).
* Создание и использование **функции** `calcDiscount(price)`.

#### 🏠 Домашняя работа (Цель: Добавить базовую функциональность на сайт)
* Подключить **`script.js`** и проверить работу через `console.log`.
* Работа с **массивами**:
    * Создать массив по теме проекта.
    * Добавить элемент с помощью **`push()`**.
    * Использовать **`map()`** или **`filter()`** для приведения к верхнему регистру.
* Использование **условий `if...else`** для изменения текста элемента на странице в зависимости от переменной `rating`.

---

### 🧱 Блок 9: JavaScript (Объекты, Конструкторы, Прототипы)

---

#### 💻 Классная работа (Задача: Создать и использовать объекты)
* Создать **объект `car`** со свойствами и **методом** `getInfo()`.
* Добавить **геттер** и **сеттер `age`** для расчета возраста машины.
* Создать **коллекцию `Map`** для хранения пользователей.
* Добавить в **`User.prototype`** метод `increaseAge()`.

#### 🏠 Домашняя работа (Цель: Расширить функциональность сайта, используя объекты)
* Создать **объект** по теме сайта: мин. 3 свойства и 1 **метод**.
* Добавить **геттер** и **сеттер** для свойства или вычисляемого значения.
* Создать **функцию-конструктор** (например, `function Film(...)`).
* Добавить **метод в прототип**.
* Создать **коллекцию `Map` или `Set`** и вывести все элементы.

---

### 🧱 Блок 10: Тестирование (Mocha и Chai)

---

#### 💻 Классная работа (Задача: Написать тесты для функций калькулятора)
* Написать тесты для функций `add`, `subtract`, `multiply`, `divide`.
* Проверить **корректные результаты** для нескольких наборов данных.
* Проверить на **выброс ошибки** при делении на ноль.

#### 🏠 Домашняя работа (Цель: Написать тесты с помощью Mocha и Chai)
```
function add(a, b) {
 return a + b;
}
function subtract(a, b) {
 return a - b;
}

function multiply(a, b) {
 return a * b;
}

function divide(a, b) {
 if (b === 0) throw new Error("Division by zero");
 return a / b;
}

module.exports = { add, subtract, multiply, divide };
```
* Подключить **Mocha** и **Chai** к своему проекту.
* Написать **2-3 теста** по своему проекту (для функций или методов, созданных ранее).

---

### 🧱 Блок 11: DOM и `localStorage`

---

#### 💻 Классная работа
* Создать заголовок `createElement('h1')`
* Создать список `<ul>` с тремя начальными элементами `createElement('ul')`
* Создать кнопку с текстом «Добавить / удалить элемент»
* При каждом нажатии на кнопку:
чередовать действия – добавлять и удалять по очереди

#### 🏠 Домашняя работа (Цель: Переключатель темы страницы с сохранением режима)
* Добавить **кнопку** "сменить тему".
* Стилизовать: **светлая тема** (фон/текст), **тёмная тема** (класс `.dark` на `<body>`).
* JS: Найти кнопку, при клике **`classList.toggle`** для класса `.dark`.
* **Сохранить** выбранную тему в **`localStorage`**.

---

### 🧱 Блок 12: Итоговые задачи

---

#### 💻 Классная работа
* *Задачи отсутствуют*

#### 🏠 Домашняя работа (создать Cookies, использовать IndexedDB.)
1. Создать `cookie user="student"` на 1 час (3600 секунд)
    - Проверить в консоли, что cookie установлено
    - Удалить `cookie`, используя `max-age=0`
2. Создать небольшую базу данных `"notesDB"` версии 1
    - Создать `object store "notes"` с ключевым полем `"id"`
    - Добавить запись `{ id: 1, text: "Моя первая заметка" }`
    - С помощью курсора `(openCursor)` вывести все записи в консоль

---

### 🧱 Блок 13: Функции и прототипное наследование

---

#### 💻 Классная работа
* *Задачи отсутствуют*

#### 🏠 Домашняя работа (создать Cookies, использовать IndexedDB.)
* Задача: создать калькулятор формул
1. Дана функция:
```
function createCalc(expression) {
 return new Function("x", `return ${expression}`);
}
```
- Необходимо превратить её в полноценный инструмент для вычисления формул
2. Несколько переменных и значения по умолчанию
    - Требуется, чтобы функция поддерживала запись вида:
    ```const calc = createCalc("x + y * z", { x: 2, y: 3, z: 4 });
    console.log(calc()); // 14
    console.log(calc({ x: 10, z: 2 })); // 16
    ```
    - Второй параметр – объект значений по умолчанию
    - Функция, которую вы возвращаете, должна принимать объект переопределений
    - Для объединения значений используйте spread
    - Чтобы передать переменные в new Function, можно использовать:
    `const keys = Object.keys(finalValues);`
    `const values = Object.values(finalValues);`
3. Сделайте так, чтобы:
    - expression и defaults нельзя было изменить напрямую, но можно было прочитать
    через методы
    `calc.getExpression(); // строка выражения`
    `calc.getDefaults(); // объект значений`
    - Не сохраняйте данные в свойства самого `calc`
    - Используйте переменные внутри `createCalc`
    - Методы должны иметь доступ к этим данным через замыкание
4. Дополнительное задание
    - Поддержка Math-функций:
    `createCalc("sin(x) + cos(y)");`
    - Подсказка:
    - Можно обернуть выражение в:
   `with (Math) { return ... }`

---

### 🧱 Блок 14:

---

#### 💻 Классная работа
* *Задачи отсутствуют*

#### 🏠 Домашняя работа
* Задача: создать модуль, который вызывает пользовательские события при действиях
пользователя

1. Создать пользовательское событие
Написать функцию `createClickEvent()` которая должна:
    • создавать `CustomEvent` с именем `"user-click"`
    • событие должно содержать в `detail` координаты курсора `(clientX, clientY)`
    • возвращать готовый объект события
    • Пример использования:
    ```const ev = createClickEvent(100, 200);
    element.dispatchEvent(ev);```
2. Реализовать обработчик события мыши
Добавить обработчик `document.addEventListener("click", ...)`
При клике:
    • вы должны создать CustomEvent через `createClickEvent(e.clientX, e.clientY)`
    • и вызвать `dispatchEvent` на `document.body`
3. Сделать слушатель пользовательского события
Добавить:

```
document.body.addEventListener("user-click", e => {
console.log("Клик зафиксирован:", e.detail);
});
```
4. Создать пользовательское событие `"key-pressed"`, которое:
    • вызывается при `document.addEventListener("keydown", ...)`
    • содержит в `detail` значение `e.key`
5. Дополнительное задание
Реализовать `Drag’n’Drop` (можно в своем проекте)

---

### 🧱 Блок 15:

---

#### 💻 Классная работа
* *Задачи отсутствуют*

#### 🏠 Домашняя работа
* Задача: создать модуль, реализующий базовые классы пользователей и обработку вебформы с валидацией, асинхронной отправкой и пользовательскими событиями

1. Создать класс `User` с полями:
    • `name` (публичное поле),
    • #password (приватное поле),
    • методом `checkPassword(pass)`, возвращающим `true/false`
2. Создать класс `Admin`, наследующий `User`, добавьте статическое свойство `role = 'admin'` и метод `resetPassword()`, который генерирует новый случайный пароль и
записывает его в приватное поле
3. Продемонстрируйте работу:
    • создайте экземпляр `User` и `Admin`,
    • проверьте пароль,
    • вызовите `resetPassword()`,
    • выведите в консоль статическое свойство и результат работы `isAdmin`
4. Создайте простую HTML-форму с полями:
    ```• <input type="text" name="email">
    • <input type="password" name="pass">
    • <button type="submit">Send</button>```
5. Напишите JS-код, который:
• Вешает обработчик `submit с e.preventDefault()`
По нажатию кнопки:
1. Проверьте валидность `email` (простая
проверка через `includes('@'))`
2. Если некорректно – выводите `alert` и не
отправляйте
3. Если корректно – создайте `Promise`,
имитирующий “отправку данных”
(`setTimeout` 1 сек)
4. После выполнения промиса выводите
сообщение об успехе
    `.document.body.addEventListener("userclick", e => { console.log("Кликзафиксирован:", e.detail); });`